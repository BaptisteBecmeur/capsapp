<%= form_for([@prestation, @prestation.reviews.new]) do |f| %>
  <%= f.text_field :star %>
  <%= f.text_field :comment %>
  <%= f.hidden_field :prestation_id, value: @prestation.id %>
  <div class="actions"%>
    <%= f.submit %>
  </div>
<% end %>
